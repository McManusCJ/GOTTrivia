  <div class="mdc-card">
    <div class="mdc-card__horizontal-block">
      <section class="mdc-card__primary">
        <h1 class="mdc-card__title mdc-card__title--large">Beibi</h1>
        <h2 class="mdc-card__subtitle">Qpi nenes</h2>
      </section>
      <img class="mdc-card__media-item mdc-card__media-item--1x" src="image.jpg"></img>
   datos del bebe
      <section class="mdc-card__actions">
        <button class="mdc-button mdc-button--compact mdc-card__action">Juega solito :3</button>
        <button class="mdc-button mdc-button--compact mdc-card__action">Vete alv<3</button>
      </section>
    </div>
  </div>
  <% for(perfil of perfils){%>
    <ul>
      <li onclick="cambia()" id="<%= perfil.nombre %>">
         <%= perfil.nombre %>
      </li>
    </ul>
  <% } %>
    <script>
    function cambia(){
      console.log("hoad");
    }
    </script>
